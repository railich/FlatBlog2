<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label 'comment' %>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
  
<% end %>